services:
  live-logs:
    build: .
    ports:
      - "8081:8081"
    environment:
      PORT: 8081
      ROOM_HISTORY_SIZE: 500
      SETTINGS_FILE: /app/data/settings.json
    volumes:
      - ./data:/app/data
    restart: unless-stopped
